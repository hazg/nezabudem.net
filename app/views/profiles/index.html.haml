%h1 Участники проекта Nezabudem.NET
= paginate @profiles
%table.table.profiles
  %tr
    %th Аватар
    %th Ник
    %th Оцифрованно имен
    %th Объектов
    %th Фотографий

  - @profiles.each do |profile|
    %tr
      %td= image_tag( profile.user.avatar.photo.icon )
      %td= link_to( profile.user.nick, profile )
      %td= profile.user.soldiers_count
      %td= profile.user.places_count
      %td= profile.user.place_photos_count

= paginate @profiles

:coffeescript
  $('table.table.profiles td').each ()->
    $(this).click ()->
      window.location = $('a', this).attr('href')
